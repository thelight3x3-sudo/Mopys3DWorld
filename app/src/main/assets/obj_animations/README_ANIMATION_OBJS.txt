FIX14 uses the newly uploaded volumetric chibi model as the in-game runtime mesh.
The attack animation is now a cooldown-controlled rigid punch effect in Java/OpenGL.
Old generated pose OBJ files were removed so the project does not mix the previous player model with the new one.
